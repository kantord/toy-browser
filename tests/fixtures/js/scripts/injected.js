// Inserted by another script, so it never appears in the parsed markup.
const line = document.createElement("div");
line.className = "line";
line.textContent = "injected script ran";
document.getElementById("stage").appendChild(line);
