# Lessons

One file per finding kind, named after it: a `file-too-long` finding looks for
`file-too-long.md`.

Empty on purpose. A lesson is written only after a grilling session has settled
how this repo handles that kind — see `../SKILL.md`. An agent that meets a
finding with no lesson here is expected to stop and ask, not to guess.
