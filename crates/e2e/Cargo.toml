[package]
name = "toy-browser-e2e"
description = "Internal tool: runs the browser on a real X11 desktop and reads it back through AT-SPI."
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
anyhow.workspace = true
testcontainers = { version = "0.28.0", features = ["blocking"] }

[lints]
workspace = true
