[package]
name = "toy-browser-fetch"
description = "One shared, thread-safe, cached place every byte is read from."
version.workspace = true
edition.workspace = true

[dependencies]
ureq = { version = "3.4.0", default-features = false, features = ["rustls"] }
url = "2.5.8"

[lints]
workspace = true
