/target
/out
node_modules/
test-results/
playwright-report/

# Findings to take upstream, not part of this repo's history.
TAKUMI-ISSUES.md

# Fetched by `just wpt-setup`, not ours to carry.

# Fetched into a container volume by `just wpt`, not ours to carry.
tests/wpt/checkout/

# Python build artifacts from the wptrunner product package.
tests/wpt/product/build/
tests/wpt/product/*.egg-info/
